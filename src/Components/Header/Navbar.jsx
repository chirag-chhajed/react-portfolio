export default function Navbar(){
    return(
        <nav>
            <a href="#about">About</a>
            <a href="#projects">project</a>
            <a href="#contact">contact</a>
        </nav>
        )
}